#include <stdio.h>
#include <stdlib.h>

// Function handlePrompt
// Description: Prompts user for input
// Parameters: char* prompt, int tries
// Returns: int number
int handlePrompt(char *prompt, int tries);

// Function handleMenuPrompt
// Description: Prompts menu for menu input
// Parameters: char* prompt
// Returns: int number
int handleMenuPrompt(char *prompt);